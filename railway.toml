[build]
buildCommand = "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y && source ~/.cargo/env && cargo install wasm-pack && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"

[environment]
NODE_VERSION = "18" 